const config = {
    // BACKEND_API: 'http://localhost:8000', // Change this URL as needed
    BACKEND_API: 'https://newsly-backend-api.onrender.com', // Change this URL as needed
    
};

export default config;